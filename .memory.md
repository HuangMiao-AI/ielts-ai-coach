# IELTS AI Coach Project Memory

Last updated: 2026-08-30

## Current state

The active local branch is `audit/pre-deployment-bug-audit-v1`, based on
`a74ce1f5e8c6a8b738572176a7c3e44714d29f3d`. The pre-deployment audit changed
only confirmed application bugs and their regression tests. No merge, push,
deployment, database/schema change, or migration is part of this branch. The
active V1 remains a Python 3.12 Streamlit app; `archive/enterprise-v0/` remains
read-only.

## Listening Vocabulary Lab

- The normal authenticated and guest Listening route now opens a session-only
  vocabulary and spelling trainer. The previous Mini Practice implementation,
  data, audio assets, and tests remain intact but are no longer reachable from
  the normal Listening route.
- The project-curated local bank contains 300 unique B1-C1 entries across 11
  IELTS-relevant categories. Each entry has a stable ID, English word, Chinese
  meaning, part of speech, level, and category; it is not presented as an
  official IELTS word list.
- Each learning round selects 10 unique words. The spelling challenge can reuse
  the most recently studied set, normalizes only surrounding whitespace and
  case, awards 10 points per exact answer, rejects duplicate submissions, and
  provides a summary, wrong-word review, and replay.
- Pronunciation uses the browser Web Speech API with en-GB, en-US, then other
  English voice preference. It handles asynchronously loaded voices, cancels
  prior speech on replay/word/page changes, and shows a clear fallback when
  speech or an English voice is unavailable.
- Guest and signed-in Listening progress is scoped to the current Streamlit
  session and never writes users, profiles, results, analytics, or Training
  Arena score. Stale or malformed session objects are discarded safely.

## Existing guest, score, and Writing behavior

- The one-click guest entry, session-only Training Arena score, and existing
  login/register behavior remain unchanged. Academic Task 1 provides six
  project-original local SVG prompts: a
  renewable-energy line graph, library-visits bar chart, household-spending
  pie charts, community-transport table, rainwater-harvesting process, and
  Riverside Park before/after map.

## Pre-deployment audit fixes

- Switching an Academic Writing Task 1 visual now saves the outgoing editor
  value into the per-visual draft registry inside the selectbox callback. This
  preserves text entered immediately before the same rerun instead of relying
  on an earlier independent rerun.
- Signed-in primary navigation now exposes the existing Settings route on
  desktop and mobile. The route contains the existing logout action, so users
  can end an authenticated session through normal navigation.
- Regression coverage proves both fixes. Do not weaken the same-rerun Writing
  switch test by inserting a separate editor rerun before changing the visual.

## Verification and data safety

- The full 334-test pytest suite passes. Python compilation, dependency, and
  whitespace checks pass.
- Browser acceptance covered guest entry, Training Arena exact-once scoring,
  Reading answer/submit/review flows, Listening study/spelling/review flows,
  Writing Task 1 and Task 2 draft/pause/submit flows, authentication,
  Settings/logout, analytics, refresh/navigation, and 390/768/1440 layouts
  without horizontal overflow. Browser checks used temporary SQLite databases.
- The real development database, WAL, and SHM files were not opened or changed;
  automated and browser validation used isolated temporary databases. Guest
  browser workflows left all persistent user, profile, score, plan, attempt,
  essay, and analytics tables empty.
- Reading currently emits a Streamlit deprecation warning for
  `st.components.v1.html`, which is supported by the pinned Streamlit version
  but should be migrated before a future Streamlit upgrade. An authenticated
  Reading start also produced a repeatable frontend `MutationObserver` console
  exception with no visible functional failure and no project-owned
  `MutationObserver`; track it as a compatibility risk, not a confirmed product
  bug.
- Do not commit databases, WAL/SHM files, backups, secrets, logs, virtual
  environments, generated caches, screenshots, or archive files.

## Next step

Review the completed audit commit, then decide separately whether to merge,
push, deploy, migrate data, or add persistent vocabulary progress. None of
those actions is approved in the current task. For a public deployment, verify
the selected host's persistent single-instance SQLite storage and Web Speech
behavior in its supported production browsers.

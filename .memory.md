# IELTS AI Coach Project Memory

Last updated: 2026-08-27

## Current state

The active local branch is `feature/guest-score-writing-visuals-v1`, created
from `a43a93d27decd55da262ab9e9bce3ccf9176ae5f`. No merge, push, database
schema change, or migration is part of this branch. The active V1 remains a
Python 3.12 Streamlit app; `archive/enterprise-v0/` remains read-only.

## Guest access and Training Arena score

- The public page now offers a prominent one-click guest entry while retaining
  the existing login and registration flows. Guest identity is a random,
  browser-session-only namespace and never creates a database user or profile.
- Guest routes are deliberately limited to database-free Dashboard, Training
  Arena, Reading, Listening, and Writing flows. Guest answers, bilingual
  reviews, drafts, results, and controls stay in Streamlit session state.
- Training Arena scores accumulate exactly once per completed round in a
  participant-scoped session key. Both guests and signed-in users display the
  honest `本次积分` chip because the existing schema has no reliable persistent
  Arena score; no permanent score is inferred or fabricated.

## Academic Writing Task 1 visuals

- Academic Task 1 now provides six project-original local SVG prompts: a
  renewable-energy line graph, library-visits bar chart, household-spending
  pie charts, community-transport table, rainwater-harvesting process, and
  Riverside Park before/after map.
- Each task keeps its own prompt, controller, draft, and result state. The page
  order is visual, instructions, then editor; assets scale without horizontal
  overflow at 390px. General Task 1 and both Task 2 flows remain unchanged.

## Verification and data safety

- The full 312-test pytest suite passes. Python compilation, dependency checks,
  and whitespace validation pass.
- Browser acceptance covered one-click guest entry, desktop and 390px layouts,
  all six visual choices, visual/prompt ordering, draft retention across page
  navigation, Writing pause/resume, a live Listening session/audio, and score
  display. Browser tests used a temporary SQLite database.
- A read-only logical dump of the live development database matches the
  pre-task same-day backup fingerprint. An early test connection checkpointed
  SQLite WAL files physically, but logical data and schema did not change; all
  later automated and browser validation uses isolated temporary databases.
- Do not commit databases, WAL/SHM files, backups, secrets, logs, virtual
  environments, generated caches, screenshots, or archive files.

## Next step

Review this completed local branch, then decide separately whether to merge,
push, deploy, or migrate data. None of those actions is approved.
